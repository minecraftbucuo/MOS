# Limine 引导器配置（v8+ 新语法：键冒号空格值；启动项以 / 开头）

# 开机菜单等待秒数，0 = 不显示菜单直接进第一项
timeout: 0

# 启动项：MOS
/MOS
    # 使用 Limine Boot Protocol（我们的内核就是按这个协议链接的）
    protocol: limine

    # 内核路径。boot(): = limine.conf 所在的那个分区/卷，
    # 后面接内核在镜像里的路径
    kernel_path: boot():/mos-kernel
