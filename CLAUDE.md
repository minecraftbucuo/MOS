# MOS — 自制操作系统项目

## 项目概况

- 目标：用 Rust 在 x86_64 上从零写一个教学性质操作系统，UEFI 启动，QEMU 开发、真机（U 盘）可运行
- 技术栈：Rust（no_std）、Limine Boot Protocol、QEMU（OVMF）、xorriso
- 技术方案与分阶段路线图：见 `docs/` 教程目录

## 协作模式（重要）

本项目以**学习**为目的，用户要亲手做，Claude 只做小步引导：

- 命令/安装：给用户自己执行（用户的终端是 **fish shell**，bash 语法要翻译），并解释作用
- 代码：每次只写一小块（1~2 个文件），配逐段讲解：这段在系统里的位置、关键写法为什么如此
- 每阶段先讲概念再动手；让用户自己跑构建、读报错，Claude 引导而非代劳
- 教学知识沉淀到 `docs/` 目录，按 `docs/README.md` 的章节结构组织，用户在整理成教程

## Git 约定

- 提交信息用**中文**，遵循：`<类型>: <描述>`，如 `文档: 添加启动流程章节`、`内核: 实现串口输出`
- **提交时机**：改动完成后不要立刻提交——用户读完、确认满意、并开始下一个任务时再提交。用户没验收的内容绝不先提交

## 环境

- Arch Linux，用户终端为 fish shell
- 开发工具：qemu-full、ovmf、xorriso、wget、rustup（stable）
- Rust 镜像：rsproxy.cn（用户网络环境需要）
